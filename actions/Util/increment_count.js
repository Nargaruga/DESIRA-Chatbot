  /**
   * Increments the temp.count variable, setting it to 1 if not yet defined.
   * @title Increment count
   * @category Utility
   */
    if (temp.count == null) {
      temp.count = 1
    } else {
      temp.count = temp.count + 1
    }